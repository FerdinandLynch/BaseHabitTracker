export function RuleList() {
  return (
    <ul className="rule-list">
      <li>Each completion records one on-chain increment.</li>
      <li>Progress is tied to your connected wallet address.</li>
      <li>Current contract tracks a simple on-chain completion count.</li>
      <li>Multi-habit tracking and streak logic can be added in a future upgrade.</li>
    </ul>
  );
}
